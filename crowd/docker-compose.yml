crowd:
  hostname: crowd
  build: docker/crowd
  ports:
    - "8095:8095" # no cache
  links:
    - database
  volumes:
    - home:/opt/crowd-home

database:
  hostname: database
  build: docker/postgres
  volumes:
    - tmp:/tmp # import backups
    - data:/var/lib/postgresql/data
